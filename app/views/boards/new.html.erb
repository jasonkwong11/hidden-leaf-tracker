<div class="board-form" style="margin-left:20px; text-align: center">
  <div class="board-column">
    <%= form_for(@board, url: boards_path) do |f| %>
      <h1 class="heading-title">
        Create a New Board
      </h1>
      <div class="flash-message" style="color:blue;">
        <% if flash[:notice] %>
          <div class="notice"><%= flash[:notice] %></div>
        <% end %>
      </div>
      <div class="board-form-container">
        <div class="board-form__board-name">
          <%= f.label :name, "Board Name" %>
          <%= f.text_field :name %>
        </div>
      </div>
      <!-- create booking board buttons -->
      <div class="board-form-container">
        <div class="form-field">
          <button type="submit" name="commit" class="btn btn-primary btn-lg">Create Board</button>
        </div>
        <%= link_to 'Back to All Boards', :root, class: 'btn btn-primary btn-md' %>
      </div>
    <% end %>
</div>